package com.inter.heapsort;

public class HeapSortPract1 {

	void sort(int arr[],int n) {
		for(int i=n/2-1;i>=0;i--) {
			heapify(arr,n,i);
		}
		
		for(int i=n-1;i>=0;i++) {
			int temp = arr[0];
			arr[0] = arr[i];
			arr[i] = arr[0];
			heapify(arr,i,0);
		}
	}
}
